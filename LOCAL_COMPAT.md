# New API 官方 QQ 身份兼容修复

适用部署：AstrBot 4.28.1、astrbot_plugin_newapi_tx 1.0.50。本仓库是已部署插件的源码快照和本地兼容补丁，不含配置、API 令牌、数据库密码或用户绑定数据。上游插件升级可能覆盖补丁，升级后请重新对比并运行回归测试。

## 指令

管理员命令仍需要 **AstrBot 管理员权限**；New API 管理员令牌只负责插件访问网站，不会自动授予聊天用户管理权限。

| 用法 | 行为 |
| --- | --- |
| `查询 13` | 按网站 ID 查询 QQ/OpenID 绑定及绑定时间 |
| `查余额 13` | 查询该网站账号余额 |
| `调整余额 13 1.25` | 增加 1.25 显示额度 |
| `调整余额 13 -1.25` | 扣减 1.25 显示额度 |
| `打劫 13` | 以该网站账号为目标（要求双方已绑定且满足次数/冷却等限制） |
| `解绑 13` | 恢复分组并删除该网站 ID 的 QQ/OpenID 绑定 |
| `查询余额` / `签到` | 根据发起人的平台身份查找自己的绑定 |

目标也支持 OpenID 和真实成员提及。文本数字保留“网站 ID 优先，再尝试 QQ 号”的原有规则，但网站 ID 会查两张绑定表。明确的 @ 只按平台身份查询，不会转成碰巧相同的网站 ID。需要消歧义时可输入 `qq:数字`、`openid:身份`、`site:网站ID`。

官方 QQ 的目标只取顶层 `mentions` 中明确提供的成员身份，排除机器人所有已知身份别名、重复项和仅显示昵称的文本。不从 author、引用消息或昵称猜测目标。AstrBot 4.28.1 原生只把机器人自身提及转成 At；本插件在自身目标命令提交前为其他明确成员补 At，并只清理已确认身份的正文标签，保留 SDK 对象与原始数据。只有正文标记而没有成员映射时，保守地交给诊断显示，不把无法排除的机器人标记认作目标。

**更正此前 parser 结论：**实际 SDK 的 ConnectionState 在 `_bot_login` 时创建，晚于 AstrBot 构造期间注册类方法。用真实适配器及模拟 HTTP 登录验证，未加载 NewAPI 插件时 `group_message_create` 已存在、也能保留成员 mentions。此前“运行中 parser 不稳定缺失”的判断没有证据，旧登录/parser 补丁现已移除。日志中的“parser registered”只证明代码执行，不能证明修复了成员目标。

补丁放在持久化插件的 `qq_compat.py`，成员提及修复仅包装 QQ client 实例的两个群回调和同步 `_commit`，不修改镜像源码或 SDK parser。身份补全只处理本插件的 `查询`、`查余额`、`调整余额`、`打劫` 和 `提及诊断`；下述免 @兼容另外包装原生命令过滤器。卸载时撤销自有包装；第三方后续包装存在时，已失效的本包装透传。初始化异常或取消时也清理包装。旧版本迁移需重启进程，清掉旧 `_bot_login` 闭包。

### QQ 群标准插件指令免 @（2026-09-24）

**更正此前唤醒结论：**AstrBot 4.28.1 的 `CommandFilter` / `CommandGroupFilter` 仍要求 `is_at_or_wake_command`。本地 `wake_prefix=["/"]` 时，收到普通群消息不等于裸指令自动执行。QQ 是否实际推送不带机器人提及的群消息，与这一框架门槛是两个独立问题。

本补丁对当前 `qq_official` WebSocket 适配器的真实 SDK `GroupMessage`（群消息类型、非空且相符的 `group_openid`）启用标准命令免 @。同步调用原生命令过滤器期间临时放行唤醒标记，并在 `finally` 中恢复，不改写角色、不自行执行 handler、不改变唤醒前缀。标准命令的完整名称、别名、子命令及参数解析仍由原生处理；权限、群白名单、插件启停、会话限制均保留。命令名称须完整匹配或后接空白，不把 `打劫2` 等近似文本当作 `打劫 2`。

此兼容在 NewAPI 插件加载期间作用于该 QQ 群通道中所有使用原生命令过滤器的已启用插件，而非仅限 NewAPI 命令。它不会打开普通聊天的默认 LLM 回退，即使命令无回复或被会话过滤移除也不回退；插件明确发出的模型请求仍保留。原生 @机器人、`/` 或其它已配置前缀的行为不变。命令中的成员 At 参数可保留，但不凭空恢复 QQ 未下发的身份。不扩大到 Webhook、频道、私聊或以 @其他成员、Reply、AtAll 开头的裸消息；插件自行增加的唤醒限制仍可能拒绝。裸指令 handler 异常继续走原生停止逻辑，但原生异常文本也受真实唤醒标记限制，因此不会伪造 @来强制显示。

`[NewAPI QQReceive]` 只记录固定事件类型与计数，分别在第 1、2、3、10、100、1000 次输出，用于区分全量 `GROUP_MESSAGE_CREATE` 与提及 `GROUP_AT_MESSAGE_CREATE`。`[NewAPI QQCommand] bare_filter_matched=True` 每次生命周期最多输出 20 次，只证明过滤器匹配，不证明权限/会话通过、业务成功或消息发出。日志不含正文、账户、群号或令牌。

现场只用无资金影响的 `pingapi` 验收：先不 @发送 `pingapi`，再不 @发送 `/pingapi`，必要时以 `@机器人 pingapi` 为对照。离线通过、加载日志和源码哈希都不能代替真实 QQ 收发成功。若不 @消息根本没到 SDK 回调，此本地补丁不能使 QQ 下发消息；须再核实当前群的实际接收权限，不把缺少事件直接说成已确认的群设置值。


### 只读提及诊断

在实际群内发送 `@机器人 提及诊断 @成员`。命令不读写网站账户、绑定或额度，只显示事件类型、原始字段是否存在、SDK/raw mentions 数、正文标签数、新增 At 数、最终目标数，并用当前连接授权执行一次 QQ `GET /v2/groups/{group_openid}/bot_state`。接口查询全局间隔至少 3 秒；错误响应与身份不输出。`11253` 表示没有查询此接口的权限，不能据此判断接收模式关闭。无当前连接凭据时不重新登录。

为避免排查依赖用户记住新命令，`查询`、`查余额`、`打劫` 等经 `_resolve_target` 判定目标缺失时，会自动异步查询当前群 `bot_state`，不阻塞命令、不额外发群消息。日志 `[NewAPI QQState]` 只包含关联序号和接收模式枚举（含 `permission_denied_11253` / `unavailable`），不输出群或用户身份。同一群至少间隔 30 秒、全局至少间隔 10 秒、只允许一个查询在途；只在内存暂存最多 30 个群的限频状态，卸载清除并取消/等待在途任务。无连接凭据、已有有效目标时不查询。

`enable_group_c2c=True` 只是群/C2C订阅开关，不等于群主已授权全量接收。`recv_msg_setting=all` 也仅证明接收范围，不保证 mentions 完整。普通事件的现行官方文档同样列有 mentions，不能认定只有新事件才支持成员身份。

新增 `[NewAPI QQMention]` 日志仅记录固定事件枚举、布尔值及计数，每次插件生命周期最多 60 条消息；不写原始正文、昵称、OpenID、群号、消息 ID 或令牌。历史普通 event_bus 消息链不足以单独证明原始 mentions 是否存在。

参考：[AstrBot v4.28.1 解析代码](https://github.com/AstrBotDevs/AstrBot/blob/v4.28.1/astrbot/core/platform/sources/qqofficial/qqofficial_platform_adapter.py#L825-L878)、[SDK 历史 issue #168](https://github.com/tencent-connect/botpy/issues/168)、[普通群事件](https://bot.q.qq.com/wiki/develop/api-v2/autogen/event/group_at_message_create.html)、[群接收状态](https://bot.q.qq.com/wiki/develop/api-v2/autogen/api/v2_groups_group_openid_bot_state.get.html)。SDK 历史 issue 不能代替当前账号 payload 验收。

## 修复范围

- 补全网站 ID 对 `newapi_openid_bindings` 的查找，影响打劫、调整余额、查余额和查询绑定。
- 统一平台身份判断、排除机器人自身的提及，保留全数字 OpenID；多个目标拒绝执行单账号操作。
- 调整余额使用当前框架的 `GreedyStr` 接收完整参数，支持包含空格的提及昵称，拒绝非有限值、零和小于一个原始额度单位的值。
- 额度已调整但读取新余额失败时单独提示，不再误报为未执行而诱导重试。
- 查询绑定支持仅 OpenID 及双绑定，显示身份类型，兼容 MySQL/SQLite 时间值。
- 显式嵌套配置读取，统一额度换算、时区、官机 Markdown、调试和其他已配置开关。
- 个人红包的用户令牌验证发送绑定网站 ID 对应的 `New-Api-User`，使用用户令牌并核对返回身份，不借用管理员令牌。
- 仅在当前绑定 INSERT 成功后回滚，删除条件同时包含身份和网站 ID；数据库写入失败不能提示绑定成功。
- OpenID 解绑也恢复分组；双绑定删除使用事务，任一步失败回滚并报告失败。
- 修复 SQLite 打劫冷却时间类型，打劫成功/失败及余额调整后按正确账号更新余额榜缓存。

已有管理员请求头补丁保留。部署不修改 API 地址、令牌、数据库配置、用户分组/余额或现有绑定。

## 验证

本次完整离线回归发现 96 个用例，92 个通过，4 个独立 MySQL 用例因未启用一次性测试数据库而跳过。免 @测试使用真实 AstrBot Wake→Process→StarRequest/命令过滤链，隔离内存注册表与会话边界、模拟发送/provider，强制禁止 socket 和 HTTP；覆盖权限、会话禁用、普通聊天、无回复、异常、别名/子命令/空白、成员 At、原有前缀、作用域、并发与卸载。另覆盖日志脱敏/有界及启动异常或取消后的完整撤销。原有身份/自动群状态查询回归保留。测试没有调用线上资金或绑定操作；合成测试不能代替真实 QQ 无 @收发与成员身份下发验收。

`tests/verify_live_readonly.py` 是显式运行的线上只读验收脚本，不属于默认 unittest 自动测试；收集该子进程输出时只显示 `VERIFY_RESULT=` 行，过滤插件原有详细业务日志。

- `tests/test_core_identity.py`：内存 SQLite 核心逻辑，模拟 HTTP（真实 HTTP 默认阻断）。
- `tests/test_commands.py`：真实 AstrBot CommandFilter、消息组件、命令处理器；内存数据和模拟额度接口。
- `tests/test_mysql_identity.py`：仅在 `COMPAT_TEST_MYSQL=1` 时运行，固定连接独立的 `astrbot-newapi-compat-testdb/compat_test` 测试容器，包含故障注入事务回滚。
- 测试没有读取部署凭据，也没有改变线上余额。线上验收只读验证目标查找、New API 用户读取及服务日志。

在具备 AstrBot 及插件依赖的测试环境中运行：

```bash
PYTHONDONTWRITEBYTECODE=1 python -m unittest discover -s tests -v
```

## PK 功能（2026-09-24）

`PK` / `pk` 与 `接受PK` / `接受pk` 为娱乐指令，走真实余额：

- 发起：`PK 对方网站ID 金额`（也支持真实 @对方 金额）。发起方立即扣除押注。
- 应战：对方在保留时间内发送 `接受PK [发起人网站ID]`；有多局待应战时必须指定发起人，否则提示数量。应战者扣除同等押注；余额不足会明确提示「余额不足，无法进行 PK / 无法应战 PK」，不扣款、不创建或保留原状态。
- 结算：按结算瞬间毫秒最后一位，单数=挑战者胜、双数=应战者胜；胜者获得双方押注之和，败者失去押注。
- 过期：默认 300 秒（`pk_settings.expiry_seconds`，30–86400）；超时未应战自动结束并退还发起人押注。过期清扫在挑战/应战命令时惰性执行，不依赖定时器，重启不复活。
- 一致性：按参与双方加锁；应战通过原子状态翻转 claim，并发应战只有一次结算；扣款/发放失败路径尽力原路退款，无法退款时落 `EXPIRED_UNREFUNDED` / `SETTLE_UNREFUNDED` 状态并记录 critical 日志需人工核对。
- 数据表 `newapi_pk_matches` 双引擎建表，并纳入 `new-tx 导出/导入` 迁移范围。
- 测试：17 项 PK 专项（奇偶/并发/过期/退款/余额不足/解析），全部离线内存余额账本，无线上额度写入。全套回归 113 项，109 通过、4 项独立 MySQL 跳过。

## 审计边界

本次覆盖身份解析及直接关联的指令故障，没有重做经济系统。上游原有打劫次数/日志仍使用 QQ 或网站 ID 作为日志键；同一账号双通道共享限额、日志与远程额度写入的跨系统原子性仍需单独改造与历史数据迁移。当前生产配置只有 OpenID 绑定。不能将本次解析测试视为完整经济系统审计。
